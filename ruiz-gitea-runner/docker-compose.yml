version: "3.7"

networks:
  umbrel_main_network:
    external: true
  app_network:
    driver: bridge

services:
  app_proxy:
    environment:
      APP_HOST: ruiz-gitea-runner_nginx_1
      APP_PORT: 80
    networks:
      - umbrel_main_network
      - app_network
    restart: on-failure

  nginx:
    image: caioruizz/ruiz-gitea-runner-nginx:1.3.0
    depends_on:
      - backend
      - frontend
    networks:
      - app_network
    restart: on-failure

  backend:
    image: caioruizz/ruiz-gitea-runner-backend:1.3.0
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATA_DIR=/data
    volumes:
      - "${APP_DATA_DIR:-./umbrel-data}/data:/data"
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - app_network
    restart: on-failure

  frontend:
    image: caioruizz/ruiz-gitea-runner-frontend:1.3.0
    environment:
      - NODE_ENV=production
    networks:
      - app_network
    restart: on-failure
